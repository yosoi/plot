import { createContext } from "react";

const DispatchCanvasEventContext = createContext({});

export const DispatchCanvasEventProvider = DispatchCanvasEventContext.Provider;
export default DispatchCanvasEventContext;
